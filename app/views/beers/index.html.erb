<p style="color: green"><%= notice %></p>

<h1>Beers</h1>

<%= link_to 'List of breweries', breweries_path %>
<%= link_to "New beer", new_beer_path %>
<br>
<div id="beers">
  <% @beers.each do |beer| %>
    <%= render beer %>
  <% end %>
</div>
