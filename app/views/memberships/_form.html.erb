<%= form_with(model: membership) do |form| %>
  <% if membership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
        <% membership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <% if @beer_clubs.empty? %>
      No beerclubs to join. Either you are a member in all of them or none exists.
    <% else %>
      <%= form.label :beer_club_id, style: "display: block" %>
      <%= form.select :beer_club_id, options_from_collection_for_select(@beer_clubs, :id, :name) %>
      <div>
        <%= form.submit %>
      </div>
    <% end %>
  </div>

  </div>

 
<% end %>
